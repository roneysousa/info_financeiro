unit uFrmSituacaoCaixa2;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, Buttons, ExtCtrls, Grids, DBGrids, DBCtrls;

type
  TfrmSituacaoCaixa2 = class(TForm)
    Panel1: TPanel;
    pnlBotoes: TPanel;
    BtCancelar: TBitBtn;
    Panel2: TPanel;
    grdConsultar: TDBGrid;
    Label2: TLabel;
    lbl_caixa: TLabel;
    Label3: TLabel;
    lbl_VLINICAL: TLabel;
    DBImage1: TDBImage;
    procedure BtCancelarClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmSituacaoCaixa2: TfrmSituacaoCaixa2;

implementation

Uses uFrmFecharCaixa2, Udm;

{$R *.dfm}

procedure TfrmSituacaoCaixa2.BtCancelarClick(Sender: TObject);
begin
     Close;
end;

end.
